# Horizontal crossings / 2026-09-24

The current source inventory contains **41 repositories: 40 research programmes and this site**. It was re-enumerated through the connected GitHub repository list, including an empty following page, rather than copied from the former 37-entry list.

New rooms: `slk` (V), `esdm` (III), `iwe` (I), `Structural` (III). These are entry locations, not exclusive taxonomic assignments. `eco-genetic-criticality` → `egc` and `eco-genetic-warning-extensions` → `egwe` are renames. Old room fragments and stored visited IDs are normalized without creating duplicate rooms.

## Read scope

All 40 research programmes were inspected at README/positioning-document level. `horizontal-audit.json` records source paths, inspected line ranges, content blob hashes and refs; the browser uses the identical object in `horizontal-data.js`. This is a **role/interface audit, not a full code, proof, manuscript or active-branch outcome audit**. Links marked `proposed` are our candidate interfaces inferred from the documented objects, not source assertions that a common adapter is implemented.

For ESDM the main README is only a title. The programme description therefore points explicitly to the inspected development commit `e9d25912b7662d397c15b10f1851e1cc0108d8ea`. It is not labeled merged, causally validated, or empirically superior.

## Important revisions

- SLK now owns R/K/Phi and architecture-value transport. SCH identifies functional conflict; BALANCE studies persistent compromise; PAYOFF supplies mathematical provenance and independent extensions. BITA is orthogonal, with access-route/mechanism-identification questions. PAYOFF's older README wording about BITA ownership is retained as source history, not allowed to override the newer SLK/BALANCE/BITA positioning.
- Structural is separated from EOG-WF. Its reference-adequacy, EGWE future-state sufficiency, and TTF out-of-species transfer are distinct tests. Their v0.34 handoff is later work, not an active admission bypass.
- IWE is independent empirical phenology–fitness synthesis. It does not estimate or validate L/R/K/Phi. Tier C potential overlap cannot replace direct synchrony–fitness evidence.
- SDMR, ODSP, EOG and ACSP retain distinct roles. ESDM is a generative process/state/observation development programme; 284b is cross-estimand biological checking. No universal estimator or guaranteed field detection is asserted.
- FCP and CHUN are spatial and evolutionary-time counterparts. Current CHUN is Camellia; older cross-family claims are not presented as the current programme.

## Display and evidence separation

**65 curated reading relations** connect every research room to at least one relevant counterpart. Relations carry a kind, note, source and claim ceiling. They can be traversed in either direction from the optional cabinet reached through the notebook, atlas or room footer. Reverse reading does not reverse a causal arrow. There are no automatic cliques based on floor membership.

The historic 20 typed interfaces retain their IDs and statuses. Two ownership corrections have explicit before/after receipts in `D.contactRevisions`; navigating cannot change those statuses. Horizontal reading relations live separately in `D.horizontal`, each with `scientific_edge: false`. This does not mean the studies have no science; it means the website's navigational link is not evidence for a shared cause or completed validation.

The two large exterior syndrome routes and their return branches are **removed from the geometry builder**, not hidden behind CSS. The small ground court, asymmetric five-stage architecture, perspective fracture, five discoveries, bellflower botany and `PhD in zuizui` identity remain. Scientific source files and earlier daily reviews remain intact.

## Verification

The new contract tests cover all room IDs, aliases, every relation/source, runtime/audit identity, explicit ownership revision, and absence of the exterior geometry. The new browser suite traverses all room neighborhoods, samples reverse navigation, enters real rooms, checks source refs, mobile widths and modal keyboard isolation.

Existing tower, room, architecture, origin, botanical and legacy-archive suites remain in scope. Local browser validation uses the full current page inlined in Chromium because localhost navigation is blocked by environment policy; it does not verify HTTP or GitHub Pages. CI runs the unchanged real-HTTP mode separately. Screenshots are render evidence, not automated aesthetic certification or physical-device testing.
